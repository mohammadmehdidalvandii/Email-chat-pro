{
  "name": "@email-chat-pro/types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared API contracts and TypeScript types for Email-Chat-Pro (single source of truth).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "type-check": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}